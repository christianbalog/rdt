# Configuration Docker Compose
# Copier ce fichier en .env et modifier les valeurs

# URL du backend cloud (IMPORTANT: Modifier avec votre vraie URL)
BACKEND_URL=https://votre-backend.com

# En développement local, utilisez:
# BACKEND_URL=http://host.docker.internal:8000

# Configuration MQTT (garder localhost si dans docker-compose)
MQTT_BROKER=mosquitto
MQTT_PORT=1883

# Authentification MQTT (optionnel)
MQTT_USERNAME=
MQTT_PASSWORD=
